spring:
  threads:
    virtual:
        enabled: true
  data:
    redis:
        host: localhost
        port: 6379
        password: redis_password123
        jedis:
            pool:
            max-active: 10
            max-idle: 5
            min-idle: 1
            max-wait: 2000ms
  rabbitmq:
    host: localhost
    port: 5672
    username: rabbit_user
    password: rabbit_password123